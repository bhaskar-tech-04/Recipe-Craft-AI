version: '3.9'
services:
  app:
    build: .
    ports:
      - '5000:5000'
    environment:
      - FLASK_ENV=development
      - DATABASE_URL=sqlite:///recipes.db
      - SECRET_KEY=supersecretkey
    volumes:
      - ./backend:/app/backend
      - ./uploads:/app/uploads
