document.addEventListener('DOMContentLoaded', () => {
    const container = document.getElementById('historyList');
    if (container) {
        container.textContent = 'Your recipe history will appear here.';
    }
});
